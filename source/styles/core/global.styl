
/*!**
 * Global:
 * Global styles.
 *
 */


/*!*
 * Heading:
 * `.g-heading` —  Базовый класс.
 *
 * `.g-heading_level_{1-6}` — Задает стиль уровня заголовка.
 *
 * `.g-heading_first` — Убирает верхний отступ заголовка.
 *
 *
 *		@example.padded
 *		.g-heading.g-heading_level_1.g-heading_first Заголовок 1 уровня без верхнего отступа
 *		.g-heading.g-heading_level_1 Заголовок 1 уровня
 *		.g-heading.g-heading_level_2 Заголовок 2 уровня
 *		.g-heading.g-heading_level_3 Заголовок 3 уровня
 *		.g-heading.g-heading_level_4 Заголовок 4 уровня
 *		.g-heading.g-heading_level_5 Заголовок 5 уровня
 *		.g-heading.g-heading_level_6 Заголовок 6 уровня
 *
 */

.g-heading
  margin: 1.2em 0 .5em
  font-weight: 700
  line-height: 1.2

.g-heading_level_1
  font-size: 2.6em

.g-heading_level_2
  font-size: 2.3em

.g-heading_level_3
  font-size: 2em

.g-heading_level_4
  font-size: 1.8em

.g-heading_level_5
  font-size: 1.4em

.g-heading_level_6
  font-size: 1.2em

.g-heading_first
  margin-top: 0



/*!*
 * Float:
 * `.g-cf` —  Сбрасывает обтекание блоков.
 *
 * `g-float-{right|left}` —  Делает элемент обтекаемым.
 *
 *
 *		@example.padded
 *		.g-float-right Обтекание элемета слева
 *		.g-float-left Обтекание элемета справа
 *		.g-cf
 *		p Lorem ipsum dolor sit amet, consectetur adipisicing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat.
 *
 */

.g-cf
    clearfix()

.g-float
    &-left
      float: left !important

    &-right
      float: right !important


/*!*
 * Hide/Show:
 * `.g-hide` —  Прячет элемент.
 *
 * `.g-show` —  Показывает элемент.
 *
 *
 *		@example.padded
 *		.g-hide(style="display:block") Этот блок не видно
 *		.g-show(style="display:none") Этот блок видно
 *
 */

.g-
  &hide
    display: none !important

  &show
    display: block !important


/*!*
 * Align:
 * `.g-align-{left|center|right}` —  Задает выравнивание текста.
 *
 *
 *		@example.padded
 *		.g-align-left Левая выключка текста
 *		.g-align-center Центральная выключка текста
 *		.g-align-right Правая выключка текста
 *
 */

.g-align
  &-left
    text-align: left !important

  &-center
    text-align: center !important

  &-right
    text-align: right !important


/*!*
 * Font:
 * `.g-font-{bold|italic|big|small}` —  Задает форматирование текста.
 *
 *
 *		@example.padded
 *		.g-font-bold Жирный текст
 *		.g-font-italic Курсивный текст
 *		.g-font-big Большой текст
 *		.g-font-small Маленький текст
 *
 */

.g-font
  &-bold
    font-weight: bold !important

  &-italic
    font-style: italic !important

  &-big
    font-size: 120% !important

  &-small
    font-size: 80% !important


/*!*
 * Color:
 * `.g-color-{$color_text}` —  Задает цвет текста.
 *
 *
 *		@example.padded
 *		.g-color-red Красный текст
 *
 */

.g-color
  for $name, $color in $color_text
    &-{$name}
      color: $color

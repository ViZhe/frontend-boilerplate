/*
*
* Всплывающее окно (попап).
*
* .b-popup__body					- Вешается на body, чтобы убрать скролл.
*
* .b-popup							- Стандартная обертка попапа.
* .b-popup_show						- Модификатор видимого попапа.
*
* .b-popup__inner					- Элемент обертка для центрирования .b-popup__content.	
* .b-popup__shadow					- Элемент, затемняющий содержимое сайта.
* .b-popup__content					- Элемент с содержимым попапа.
* .b-popup__close					- Кнопка закрыть попап.
*
* .b-popup__header					- Первый заголовок попапа.
*
*/


.b-popup__body
	overflow hidden

.b-popup
	fixed -1000% false false -1000%
	overflow auto
	display block

.b-popup_show
	fixed 0
	z-index 200

.b-popup__inner
	display flex
	align-items center
	justify-content center
	min-height 100%
	padding 20px 0

.b-popup__shadow
	background rgba($black, 70%)
	opacity 0
	transition opacity 0.5s ease
	cursor pointer

	.b-popup_show &
		fixed 0
		opacity 1

.b-popup__content
	background $white
	flex 0 0 auto
	max-width 50%
	min-width 300px
	padding 20px
	transform translateY(-20px)
	transition transform 0.5s ease
	relative false
	box-shadow 0 0 3px $black

	if $site_border-radius != 0
		border-radius $site_border-radius

	.b-popup_show &
		transform translateY(0px)

.b-popup__close
	absolute -13px -13px false false
	cursor pointer
	size 26px
	text-align center
	background $white
	border-radius 50%
	box-shadow 0 0 3px $black

	&::after
	&::before
		absolute 3px false false 11px
		content ""
		display block
		size 4px 20px
		background $black
		border-radius 3px

	&::before
		transform: rotate(-45deg);

	&::after
		transform: rotate(45deg);

.b-popup__header
	margin-top 0
	padding-top 0


/*!**
 * Popup:
 *
 */

/*!*
 * Всплывающее окно (попап):
 * `.c-popup__outer` — Вешается на html и body, чтобы убрать скролл.
 *
 * `.c-popup` — Стандартная обертка попапа.
 *
 * `.c-popup_show` — Модификатор видимого попапа.
 *
 * `.c-popup__shadow` — Элемент подложки под попап (затемнение фона).
 *
 * `.c-popup__inner` — Элемент обертка для центрирования .c-popup__content.
 *
 * `.c-popup__content` — Элемент с содержимым попапа.
 *
 * `.c-popup__close` — Кнопка закрыть попап.
 *
 *
 *     @example.padded
 *     .c-btn.c-btn_color_blue.js-popup(data-popup=".shom-small") Покажись маленький
 *     .c-btn.c-btn_color_blue.js-popup(data-popup=".shom-big") Покажись большой
 *     .h-hide.shom-small
 *       div(style="height:200px;background-color:#939DA3;") Маленький
 *     .h-hide.shom-big
 *       div(style="height:1000px;background-color:#939DA3;") Большой
 *
 */

+media('max', 'md')
  .c-popup__outer
    overflow: hidden

.c-popup__outer > body
  overflow: hidden

.c-popup
  position: fixed
  top: 0
  right: 0
  bottom: 0
  left: 0
  z-index: 200
  visibility: hidden
  overflow: auto
  min-width: 320px
  transition: all .3s
  opacity: 0

.c-popup_show
  visibility: visible
  opacity: 1

.c-popup__shadow
  position: fixed
  top: 0
  right: 0
  bottom: 0
  left: 0
  background: rgba($black, 90%)

.c-popup__inner
  display: flex
  align-items: center
  justify-content: center
  min-height: 100%
  padding: 20px 0

  +media('max', 'md')
    align-items: stretch
    padding: 0

.c-popup__content
  position: relative
  flex: 0 0 auto
  width: 70%
  min-width: 320px
  max-width: 1000px
  padding: 15px
  transition: all .3s ease
  border-radius: auto
  background: $white
  box-shadow: 0 0 3px $black
  transform: translateY(-40px)

  +media('max', 'md')
    width: 100%

    if $borderRadiusDefault != 0
      border-radius: 0

  .c-popup_show &
    transform: translateY(0px)


.c-popup__close
  position: absolute
  top: -13px
  right: -13px
  size: 26px
  cursor: pointer
  text-align: center
  border-radius: 50%
  background: $white
  box-shadow: 0 0 3px $black

  +media('max', 'md')
    top: 0
    right: 0
    size: 40px
    box-shadow: 0 0 0 -1px

  &::after
  &::before
    position: absolute
    top: 3px
    left: 11px
    display: block
    size: 4px 20px
    content: ''
    border-radius: 3px
    background: $black

    +media('max', 'md')
      size: 8px 36px

  &::before
    transform: rotate(-45deg)

  &::after
    transform: rotate(45deg)

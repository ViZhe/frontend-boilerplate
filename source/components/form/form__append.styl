
/*!*
 * Append:
 * `.b-form__append` — Обертка для присоединения блоков к полю.
 *
 * `.b-form__addon` — Класс присоединяемого поля.
 *
 *
 *		@example.padded
 *		label.b-form__label
 *			.b-form__append
 *				input.b-form__field(type="text", placeholder="Simple text input", required)
 *				.b-form__addon.b-btn.b-btn_color_green Отправить
 *
 */

.b-form__append
    display flex

.b-form__append
    & .b-form__field
    & .b-form__addon
        margin 0

        if $siteBorderRadius != 0
            border-radius 0

    if $siteBorderRadius != 0
        & > :first-child
            border-radius $siteBorderRadius 0 0 $siteBorderRadius

        & > :last-child
            border-radius 0 $siteBorderRadius $siteBorderRadius 0

.b-form__append	.b-form__field
    min-width 10%
    flex 1


/*!*
 * Checkbox:
 * `.c-form__checkbox` — Класс на label, чтобы иконка приняла нужный вид.
 *
 *
 *     @example.padded
 *     .c-form
 *       label.c-form__label
 *         input(type="checkbox")
 *         .c-form__checkbox First checkbox
 *       label.c-form__label
 *         input(type="checkbox" checked)
 *         .c-form__checkbox Second checkbox (checked)
 *       label.c-form__label
 *         input(type="checkbox" disabled)
 *         .c-form__checkbox Third checkbox (disabled)
 *
 */

.c-form__checkbox
  background-image: svg('source/img/form/form__checkbox_off.svg', '[fill]:' + $black)

input:not(:disabled) + .c-form__checkbox:hover
  background-image: svg('source/img/form/form__checkbox_off.svg', '[fill]:' + $red)


input:checked + .c-form__checkbox
  background-image: svg('source/img/form/form__checkbox_on.svg', '[fill]:' + $blue)

input:checked + .c-form__checkbox:hover
  background-image: svg('source/img/form/form__checkbox_on.svg', '[fill]:' + $red)


input:disabled + .c-form__checkbox
  background-image: svg('source/img/form/form__checkbox_off.svg', '[fill]:' + $gray)

input:checked:disabled + .c-form__checkbox
  background-image: svg('source/img/form/form__checkbox_on.svg', '[fill]:' + $gray)

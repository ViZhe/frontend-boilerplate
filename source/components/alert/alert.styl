
/*!**
 * Alert:
 * Информационные сообщения.
 *
 * `.b-alert` — Базовый класс без модификаторов не живет.
 *
 */



.b-alert
	margin 15px 0
	padding 15px 30px
	font-size $site_font_size + 1

	if $site_border-radius != 0
		border-radius $site_border-radius

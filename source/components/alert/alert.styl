
/*!**
 * Alert:
 *
 */

.b-alert
    margin 15px 0
    padding 15px 30px
    font-size $site_font_size + 1
    color $black
    border 2px solid

    if $site_border-radius != 0
        border-radius $site_border-radius


/*!*
 * Информационные сообщения:
 * `.b-alert` — Базовый класс без модификатора цвета не живет.
 *
 * `.b-alert_{$color_alert}` — Задает цвет блока сообщения.
 *
 *
 *		@example.padded
 *		.b-alert.b-alert_blue
 *			h5.b-heading.b-heading_level_5.b-heading_first Внимание
 *			| Гречка дорожает! Срочно закупайтесь гречкой и талонами на интернет!!!
 *		.b-alert.b-alert_blue Гречка дорожает! Срочно закупайтесь гречкой и талонами на интернет!!!
 *
 */

.b-alert
    for name, color in $color_alert
        &_{name}
            background-color lighten(color, 90%)
            border-color color

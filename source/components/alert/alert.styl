
/*!**
 * Alert:
 * Информационные сообщения.
 *
 * `.b-alert` — базовый класс без модификаторов не живет.
 *
 * `.b-alert_{full|line|outline}` — Задает стиль сообщения.
 *
 * `.b-alert_{full|line|outline}_{$colors_alert}` — Задает цвет сообщения, согласно заданному набору.
 */



.b-alert
	margin 15px 0
	padding 15px 30px
	font-size $site_font_size + 1

	if $site_border-radius != 0
		border-radius $site_border-radius

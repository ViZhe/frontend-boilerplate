
/*!*
 * Color:
 * `.b-btn_color_{$color_btn}` — Задает цвет кнопки.
 *
 *
 *		@example.padded
 *		.b-btn.b-btn_color_red Нажать
 *		.b-btn.b-btn_color_green Нажать
 *		.b-btn.b-btn_color_blue Нажать
 *
 */

.b-btn_color
    for name, color in $color_btn
        &_{name}
            background-color color

            &:focus
            &:hover
                background-color lighten(color, 20%)

            &:active
                background-color lighten(color, 40%)
